{% set isIndex = tmpl == 'index' ? true : false %}
{% set tmpl = 'list' %}
{% extends isIndex ? '@MauticCore/Default/content.html.twig' : '@MauticCore/Default/raw_output.html.twig' %}
{% block mauticContent %}dialoghsm_number{% endblock %}
{% block headerTitle %}{{ 'dialoghsm.number.list.title'|trans }}{% endblock %}
{% block content %}
    {% if isIndex %}
    <div id="page-list-wrapper" class="panel panel-default">
        {{- include('@MauticCore/Helper/list_toolbar.html.twig', {
            'searchValue': searchValue,
            'searchId': 'dialoghsm-number-search',
            'action': currentRoute,
            'list_actions': include('@MauticCore/Helper/toolbar_page_actions.html.twig', {
                'templateButtons': {
                    'new': true
                },
                'routeBase': 'dialoghsm_number'
            }),
        }) -}}

        <div class="page-list">
            {{ block('listResults') }}
        </div>
    </div>
    {% else %}
        {{ block('listResults') }}
    {% endif %}
{% endblock %}

{% block listResults %}
{% if items|length %}
    <div class="table-responsive">
        <table class="table table-hover dialoghsm-number-list">
            <thead>
            <tr>
                {{- include(
                    '@MauticCore/Helper/tableheader.html.twig',
                    {
                        'checkall'        : 'true',
                        'routeBase'       : 'dialoghsm_number',
                        'templateButtons' : {
                            'delete' : true,
                        }
                    }
                ) -}}

                {{- include(
                    '@MauticCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'dialoghsm.number',
                        'orderBy'    : 'wn.name',
                        'text'       : 'mautic.core.name',
                        'class'      : 'col-dialoghsm-name',
                        'default'    : true,
                    }
                ) -}}

                <th class="col-dialoghsm-phone">{{ 'dialoghsm.number.phone'|trans }}</th>

                {{- include(
                    '@MauticCore/Helper/tableheader.html.twig',
                    {
                        'sessionVar' : 'dialoghsm.number',
                        'orderBy'    : 'wn.id',
                        'text'       : 'mautic.core.id',
                        'class'      : 'visible-md visible-lg col-dialoghsm-id',
                    }
                ) -}}
            </tr>
            </thead>
            <tbody>
            {% for item in items %}
                <tr>
                    <td>
                        {{- include(
                            '@MauticCore/Helper/list_actions.html.twig',
                            {
                                'item'            : item,
                                'templateButtons' : {
                                    'edit'   : true,
                                    'delete' : true,
                                },
                                'routeBase' : 'dialoghsm_number',
                            }
                        ) -}}
                    </td>
                    <td>
                        <div>
                            {{- include('@MauticCore/Helper/publishstatus_icon.html.twig',
                                {
                                    'item'  : item,
                                    'model' : 'dialoghsm.whatsappnumber',
                                }
                            ) -}}
                            <a href="{{ path('mautic_dialoghsm_number_action', {'objectAction' : 'edit', 'objectId' : item.getId()}) }}">
                                {{ item.getName() }}
                            </a>
                        </div>
                    </td>
                    <td>{{ item.getPhoneNumber() }}</td>
                    <td class="visible-md visible-lg">{{ item.getId() }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
    <div class="panel-footer">
        {{- include('@MauticCore/Helper/pagination.html.twig', {
            'totalItems' : totalItems,
            'page'       : page,
            'limit'      : limit,
            'baseUrl'    : path('mautic_dialoghsm_number_index'),
            'sessionVar' : 'dialoghsm.number',
        }) -}}
    </div>
{% else %}
    {{- include('@MauticCore/Helper/noresults.html.twig', {'message' : 'dialoghsm.number.noresults'}) -}}
{% endif %}
{% endblock %}
